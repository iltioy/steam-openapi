type: object
required:
    - username
    - status
properties:
    username:
        description: "The username associated with the user profile."
        type: string
        minLength: 4
        maxLength: 32
        pattern: "/(?=.*[a-z])(?=.*[A-Z])(?=.*[0-9])/"
        example: John78
